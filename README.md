Screenshots for the "watch a composer turn from another client" pull request.
Captured from a real `coddy http` build (headless Chrome, 1280x900) driven by a
stub OpenAI endpoint, so the runs are reproducible without an API key.
